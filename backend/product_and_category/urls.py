from django.urls import path
# from .views import ProductAndCategoryView
urlpatterns = [
    # path("product", ProductAndCategoryView.as_view()),
    # path("product/<int:id>/update", ProductAndCategoryView.as_view()),
    # path("product/<int:id>/delete", ProductAndCategoryView.as_view()),
]
